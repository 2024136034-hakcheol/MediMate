# AUTHORING.문학철_김성진.v0.1.0

> PillNova 팀 AI Agent 활용 기법 — MediMate 프로젝트 기준

## 팀원

- 문학철, 김성진

---

## 1. 사용하는 AI Agent

| 도구 | 용도 |
|------|------|
| Claude Code (CLI) | 코드 생성·디버깅·문서 작성 |
| Gemini API | 앱 내 약 이미지 인식 및 주의사항 요약 |

---

## 2. 문학철 — 나만의 기법: PRD-Driven Prompting

### 역할
- Gemini API 연동 (약 이미지 인식, 주의사항 요약)
- 서비스 레이어 설계 및 구현 (GeminiService, ScheduleService)

### 기법 설명

Claude Code에 코드를 요청할 때 항상 PRD 기능 번호를 기준으로 요청한다.

**구조:**
```
"PRD [기능번호] 요구사항을 구현해줘.
현재 코드: [코드 첨부]
구현 후 해당 기능의 조건을 체크리스트로 확인해줘."
```

**왜 이렇게 하는가:**
- AI가 요구사항 문서와 코드를 연결해 맥락을 잃지 않음
- 기능 누락 방지
- Q&A에서 "왜 이렇게 구현했나요?" 질문에 PRD 번호로 즉답 가능

**어떻게 동작하는가:**
1. PRD에서 구현할 기능 번호 확인 (예: F-01)
2. 기능 번호 + 현재 코드를 Claude Code에 전달
3. 생성된 코드가 PRD 조건을 만족하는지 체크리스트 요청
4. 체크리스트 통과 후 커밋

### 프롬프트 패턴

```
"PRD F-03 요구사항(로컬 알림, 복용 완료 처리)을 구현해줘.
현재 schedule_service.dart 코드는 아래와 같아. [코드 첨부]
구현 후 F-03의 모든 조건을 만족하는지 체크리스트로 확인해줘."
```

---

## 3. 김성진 — 나만의 기법: UI-First Mocking

### 역할
- Flutter UI 화면 구현 (ScanScreen, HomeScreen, CalendarScreen 등)
- 로컬 DB 설계 및 구현 (sqflite)
- 로컬 알림 구현 (flutter_local_notifications)

### 기법 설명

실제 API 연동 전에 목업 데이터로 UI를 먼저 완성한 뒤, 마지막에 실제 서비스와 연결한다.

**구조:**
```
"[화면 이름] 화면을 목업 데이터로 구현해줘.
실제 API 연동은 나중에 할 예정이니 MockData 클래스를 따로 만들어줘."
```

**왜 이렇게 하는가:**
- API 준비 여부와 관계없이 UI 개발 병렬 진행 가능
- Gemini API 호출 횟수를 아껴 무료 한도 내에서 개발 가능
- 중간발표 데모 시 API 오류와 무관하게 화면 시연 가능

**어떻게 동작하는가:**
1. MockData 클래스에 하드코딩된 약 정보로 UI 구현
2. UI 완성 후 MockData → 실제 GeminiService로 교체
3. 교체 후 실제 API 호출 테스트

### 프롬프트 패턴

```
"Flutter로 [화면 이름] 화면을 만들어줘.
표시할 데이터: [데이터 구조]
목업 데이터로 먼저 구현하고 나중에 실제 데이터로 교체할 수 있게 해줘."
```

---

## 4. 공통 암묵지 (LLM Wiki)

| 상황 | 문제 | 해결책 |
|------|------|--------|
| Gemini API 이미지 전송 | base64 인코딩 방식이 패키지마다 다름 | dart:convert의 base64Encode 사용으로 통일 |
| JSON 파싱 오류 | API가 JSON 외 텍스트 포함 | 프롬프트에 "JSON만 출력" 명시 |
| Flutter 패키지 추가 | pubspec.yaml 버전 충돌로 빌드 오류 | 패키지 추가 전 pub.dev에서 최신 호환 버전 확인 |
| 로컬 알림 설정 | Android 권한 설정 누락으로 알림 미작동 | AndroidManifest.xml 권한 설정 체크리스트 작성 |

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v0.1.0 | 2026-05-11 | 초안 작성 |
